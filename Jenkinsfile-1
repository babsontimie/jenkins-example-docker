pipeline {
  agent {
//    docker { image 'node:16-alpine' }
 // dockerContainer { image 'alpine:latest'}
docker { label 'tee-docker-agent' }
  }
  stages {
    stage('Test') {
      steps {
       sh '''node --version
        sh uname -a
         '''
      }
    }
  }
}
